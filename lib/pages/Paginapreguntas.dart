import 'package:flutter/material.dart';
//pagina preguntas
class Paginapreguntas extends StatelessWidget{
 
  
  @override
  Widget build(BuildContext context){
    return Center(
      child: Text("Prguntas", style: TextStyle(fontSize: 30),),
    );

  }
}