import 'package:flutter/material.dart';
//pagina detto
class Paginadetto extends StatelessWidget{
 
  
  @override
  Widget build(BuildContext context){
    return Center(
      child: Text("Detto", style: TextStyle(fontSize: 30),),
    );

  }
}